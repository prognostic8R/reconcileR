#' The output of a hierarchical forecast in a keys, index, outcome format.
#'
#' @format ## `widgets`
#' A data frame with 20 rows and 5 columns:
#' * `date` A date column.
#' * `country` A grouping or keys column giving the forecast hierarchy.
#' * `state` A grouping or keys column giving the forecast hierarchy.
#' * `city` A grouping or keys column giving the forecast hierarchy.
#' * `widgets` The outcome being forecasted.
"widgets"
